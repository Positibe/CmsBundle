{% extends '@PositibeCms/layout.html.twig' %}

{% from '@PositibeCore/_buttons.html.twig' import delete_confirm %}

{% block title %}Tipos de página{% endblock %}
{% block subtitle %}Editar coleción de páginas{% endblock %}
{% set menu_active = 'content' %}
{% set content_menu = 'category' %}

{% block breadcrumb_list %}
    <li class="breadcrumb-item">

        <a href="{{ path('positibe_category_index') }}">Tipos de página</a>
    </li>
    <li class="breadcrumb-item">

        {{ category.title }}
    </li>
{% endblock %}

{% block content %}
    {{ form_start(form,{'attr': {'class': 'form-horizontal inbox-compose' }}) }}
    <input type="hidden" name="_method" value="PUT"/>
    {{ include('@PositibeCms/Category/_form.html.twig') }}
    {{ form_end(form) }}
    {{ delete_confirm('positibe_category_remove', path('positibe_category_delete', {'name': category.name}) ) }}
{% endblock %}

{% block javascripts %}
    <script>
        jQuery('#positibe_category_locale').on('change', function (e) {
            location.replace('{{ path('positibe_category_update', {'name': category.name}) }}?data_locale='
            + $(this).val());
        });
    </script>
{% endblock %}